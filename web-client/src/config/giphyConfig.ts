// GIPHY API key — same "public client identifier" pattern as firebaseConfig.ts
// and the mobile app's streamConfig.ts: rate-limited per key, not a secret,
// meant to be used directly from client code.
export const GIPHY_API_KEY = 'dXecXNBTj30SicXAerxXxfaTedeOKrCM';
