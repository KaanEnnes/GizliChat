/**
 * Merkezi admin/mock auth yapılandırması.
 * NOT: Bu bir prototip mock config'idir, gerçek güvenlik sağlamaz.
 * İleride backend bağlanınca bu dosya bir API çağrısıyla değiştirilecektir.
 */
export const ADMIN_CONFIG = {
  username: 'admin',
  password: 'admin123',
};

export const MOCK_AUTH_DELAY_MS = 600;
