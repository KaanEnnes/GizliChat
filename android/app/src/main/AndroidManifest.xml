<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Konum paylaşımı (mevcut/canlı konum, @react-native-community/geolocation) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Fotoğraf/video paylaşımı (react-native-image-picker) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <!-- Sohbette gelen dosyaları İndirilenler'e kaydetmek (MessageBubble.tsx'in
         downloadFile'ı) için — API 29+'ta scoped storage zaten MediaStore
         üzerinden izin veriyor, bu sadece API 28 ve altı içindir. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <!-- Sesli mesaj kaydı + sesli/görüntülü arama (react-native-audio-recorder-player, Stream Video) -->
    <uses-feature android:name="android.hardware.camera.any" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.audio.output" />
    <uses-feature android:name="android.hardware.microphone" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Stream Video'nun WebRTC katmanı aramalar sırasında native bir foreground
         service başlatıyor; targetSdkVersion 36'da (Android 14+ davranışı) bu tipte
         izinler ayrıca deklare edilmezse servis başlatılamaz/görüşme kararsızlaşır.
         Daha önceki bir oturumda logcat'te "Missing ForegroundServicePermissions"
         uyarısı görülmüş ama hiç eklenmemişti — bkz. Changelog. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Stream Video'nun kendi Android kurulum kılavuzu (expo config plugin'i,
         node_modules/@stream-io/video-react-native-sdk/expo-config-plugin) bunları
         koşulsuz (keepCallAlive'a bağlı olmadan) her zaman ekliyor — Bluetooth
         kulaklık/hoparlöre ses yönlendirmesi için. Eksikse arama sırasında ses
         yönlendirmesi (callManager) sessizce yanlış cihaza gidebilir/hata verebilir. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Mini oyunlarda oyun-sonu titreşimi (hapticsService.vibrateMedium) için gerekli -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Arka plan mesaj bildirimi (FCM + notifee). Android 13+ (API 33) bunu
         çalışma zamanında ayrıca istemeyi zorunlu kılıyor — bkz.
         permissionsService.requestNotificationPermission(). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Uygulama içi güncelleme (updateService.ts + ApkInstallerModule.kt): yeni
         APK indirildikten sonra sistemin paket yükleyicisini açabilmek için. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Küçük yüzer mesajlaşma penceresi (FloatingChatService) — bu cihazın
         Android'in resmi Picture-in-Picture özelliğini donanım/firmware
         seviyesinde desteklemediği doğrulandığı için (pm has-feature
         android.software.picture_in_picture -> false) kendi overlay
         penceremizi yazıyoruz. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 15 dk okunmamış mesaj alarmı (AlarmEscalationModule/Receiver, opt-in,
         varsayılan kapalı) — ekran kapalıyken/uygulama öldürülmüşken bile
         Doze modundan cihazı uyandırabilen setExactAndAllowWhileIdle için
         gerekli. Android 12+ (API 31) bu izni kullanıcı Ayarlar'dan elle
         vermezse alarm sessizce atlanır (bkz. AlarmEscalationModule). -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <!-- ApkInstallerModule'ün indirilen APK'yı (cache dir) sistem paket
           yükleyicisine content:// URI olarak verebilmesi için. -->
      <provider
        android:name="androidx.core.content.FileProvider"
        android:authorities="${applicationId}.fileprovider"
        android:exported="false"
        android:grantUriPermissions="true">
        <meta-data
          android:name="android.support.FILE_PROVIDER_PATHS"
          android:resource="@xml/file_paths" />
      </provider>
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:supportsPictureInPicture="true"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
      <service
        android:name=".FloatingChatService"
        android:exported="false"
        android:foregroundServiceType="specialUse">
        <property
          android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
          android:value="floating-chat-window" />
      </service>
      <receiver android:name=".AlarmEscalationReceiver" android:exported="false" />
    </application>
</manifest>
